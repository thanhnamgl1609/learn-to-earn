exports.courseInfoURIs = Array(10)
  .fill(' ')
  .map((_, idx) => `http://course-info.com/${idx + 1}`);

exports.classInfos = Array(10)
  .fill(' ')
  .map((_, idx) => `http://class-info.com/${idx + 1}`);
